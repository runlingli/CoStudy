{
  "name": "costudy",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "setup": "npm --prefix frontend install && npm --prefix server install",
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix frontend run build",
    "start": "node server/index.js"
  }
}
